{
  "version": 1,
  "engine": "SQLite",
  "scope": "One private five-column Kanban board per account, plus account and learning state",
  "tables": {
    "users": {
      "columns": {"id": "TEXT PRIMARY KEY", "username": "TEXT NOT NULL UNIQUE COLLATE NOCASE", "email": "TEXT NOT NULL UNIQUE COLLATE NOCASE", "password_hash": "TEXT", "verified_at": "TEXT", "created_at": "TEXT NOT NULL"},
      "rules": ["username and email are unique regardless of case", "password_hash is NULL until email OTP verification and first password setup"]
    },
    "otp_challenges": {
      "columns": {"id": "TEXT PRIMARY KEY", "user_id": "TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE", "purpose": "TEXT NOT NULL", "code_hash": "TEXT NOT NULL", "expires_at": "TEXT NOT NULL", "attempts": "INTEGER NOT NULL DEFAULT 0", "consumed_at": "TEXT", "created_at": "TEXT NOT NULL"},
      "rules": ["purpose is signup or password_reset", "OTP expires after 10 minutes", "maximum five attempts"]
    },
    "sessions": {
      "columns": {"token_hash": "TEXT PRIMARY KEY", "user_id": "TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE", "expires_at": "TEXT NOT NULL", "created_at": "TEXT NOT NULL"}
    },
    "boards": {
      "columns": {"id": "TEXT PRIMARY KEY", "user_id": "TEXT NOT NULL UNIQUE REFERENCES users(id) ON DELETE CASCADE", "title": "TEXT NOT NULL", "created_at": "TEXT NOT NULL"},
      "rules": ["UNIQUE user_id enforces one board per account"]
    },
    "columns": {
      "columns": {"id": "TEXT PRIMARY KEY", "board_id": "TEXT NOT NULL REFERENCES boards(id) ON DELETE CASCADE", "position": "INTEGER NOT NULL", "name": "TEXT NOT NULL"},
      "rules": ["UNIQUE(board_id, position)", "position ranges from 0 to 4", "five rows seeded for each new board; API does not add or delete columns"]
    },
    "cards": {
      "columns": {"id": "TEXT PRIMARY KEY", "column_id": "TEXT NOT NULL REFERENCES columns(id) ON DELETE CASCADE", "position": "INTEGER NOT NULL", "title": "TEXT NOT NULL", "details": "TEXT NOT NULL DEFAULT ''", "created_at": "TEXT NOT NULL", "updated_at": "TEXT NOT NULL"},
      "rules": ["title and details are the only user-facing fields", "position preserves order within a column"]
    },
    "dashboard_tasks": {
      "columns": {"id": "TEXT PRIMARY KEY", "user_id": "TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE", "title": "TEXT NOT NULL", "subject": "TEXT NOT NULL", "priority": "TEXT NOT NULL", "done": "INTEGER NOT NULL DEFAULT 0", "position": "INTEGER NOT NULL"}
    },
    "practice_progress": {
      "columns": {"user_id": "TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE", "item_id": "TEXT NOT NULL", "known": "INTEGER NOT NULL", "primary_key": ["user_id", "item_id"]}
    }
  },
  "indexes": ["otp_challenges(user_id, purpose, created_at)", "sessions(user_id)", "columns(board_id, position)", "cards(column_id, position)", "dashboard_tasks(user_id, position)"],
  "storage": "Runtime database path from DATABASE_PATH, default data/shreehan.db; Docker volume mounted at /app/storage"
}
